# dialog-select Demos

| demoId | 名称 | 描述 | 代码文件 |
|--------|------|------|----------|
| nest-grid-multi | 表格多选 | <p>通过 <code>auto-lookup</code>、<code>lookup-method</code> 属性 和 <code>change</code> 事件设置初始化时数据的反查回显功能。</p> <br><p>表格多选场景需要设置 <code>popselector</code> 为 <code>grid</code>，<code>multi</code> 为 <code>true</code>，<code>checkRowKeys</code> 设置默认选中数据。<code>lock-scroll</code> 配置弹出窗口时是否禁用滚动条。</p ><br> | dialog-select/nest-grid-multi.vue |
| nest-grid-single | 表格单选 | <p>通过 <code>auto-lookup</code>、<code>lookup-method</code> 属性和 <code>change</code> 事件设置初始化时数据的反查回显功能。</p><br><p>表格单选场景需要设置 <code>popselector</code> 为 <code>grid</code>，<code>multi</code> 为 <code>false</code>，<code>checkRowKey</code> 设置默认选中数据。</p ><br> | dialog-select/nest-grid-single.vue |
| nest-tree-multi | 树多选 | <p>通过 <code>auto-lookup</code>、<code>lookup-method</code> 属性和 <code>change</code> 事件设置初始化时数据的反查回显功能。</p><br> <p>树多选场景需要设置 <code>popselector</code> 为 <code>tree</code>，属性 <code>multi</code> 为 <code>true</code>，<code>defaultCheckedKeys</code> 设置默认选中数据。</p><br> | dialog-select/nest-tree-multi.vue |
| nest-tree-single | 树单选 | <p>通过 <code>auto-lookup</code>、<code>lookup-method</code> 属性和 <code>change</code> 事件设置初始化时数据的反查回显功能。</p><br><p>树单选场景需要设置 <code>popselector</code> 为 <code>tree</code>，<code>multi</code> 为 <code>false</code >，<code>defaultCheckedKeys</code> 设置默认选中数据。</p><br> | dialog-select/nest-tree-single.vue |
| set-selection | 设置多选状态 | <p>通过 <code>auto-lookup</code>、<code>lookup-method</code> 属性和 <code>change</code> 事件设置初始化时数据的反查回显功能。</p><br><p>树单选场景需要设置 <code>popselector</code> 为 <code>tree</code>，<code>multi</code> 为 <code>false</code >，<code>defaultCheckedKeys</code> 设置默认选中数据。</p><br> | dialog-select/set-selection.vue |
