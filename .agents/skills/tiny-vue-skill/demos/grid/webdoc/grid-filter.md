# grid Demos

| demoId | 名称 | 描述 | 代码文件 |
|--------|------|------|----------|
| filter-default-filter | 基础筛选 | <br>        <div class="tip custom-block"><br>          <p class="custom-block-title">filter 配置</p><br>          <p>通过 <code>filter</code> 属性配置列筛选。配置项及对应关系：<br>            <ul><br>              <li><strong>layout</strong> 筛选项显示顺序，默认 <code>input,enum,default,extends,base</code>。各取值与下方配置一一对应：<code>input</code>→inputFilter，<code>enum</code>→enumable，<code>default</code>→defaultFilter，<code>extends</code>→extends，<code>base</code>→清除选项，<code>simple</code>→simpleFilter（与其它 layout 项互斥）。</li><br>              <li><strong>inputFilter</strong> 输入筛选。true 使用默认 input；传入对象可配置 <code>component</code>、<code>relations</code>、<code>relation</code>、<code>attrs</code>。</li><br>              <li><strong>enumable</strong> 是否显示枚举选项。需 layout 包含 enum 或 simple 时生效。</li><br>              <li><strong>multi</strong> 枚举是否为多选，仅在 enumable 为 true 时有效，默认 true。</li><br>              <li><strong>values</strong> 枚举数据源。数组或 <code>(params)=>Promise</code>；不传则从表格数据自动提取。</li><br>              <li><strong>label</strong> / <strong>value</strong> 枚举项显示值和实际值的字段名，默认 label、value。用于 values 数据结构的字段映射。</li><br>              <li><strong>defaultFilter</strong> 是否显示空/非空筛选。需 layout 包含 default。</li><br>              <li><strong>extends</strong> 扩展快捷筛选项数组，每项含 <code>label</code>、<code>value?</code>、<code>method</code>。需 layout 包含 extends。</li><br>              <li><strong>condition</strong> 初始筛选条件，如 <code>condition.value</code> 设置默认选中、<code>condition.relation</code> 设置默认关系。</li><br>              <li><strong>simpleFilter</strong> 简化版配置，需 <code>layout:'simple'</code>。含 <code>isDatetime</code>（日期范围）、<code>selectAll</code>（全选）、<code>datetimeConfig</code>（isDatetime 时必填）、<code>searchConfig</code>（非日期时可选，为枚举列表加搜索框）。</li><br>              <li><strong>method</strong> 自定义筛选方法，用于 <code>#filter</code> 插槽完全自定义时实现筛选逻辑。</li><br>              <li><strong>attrs</strong> 过滤面板根节点属性，如 id、class 等。</li><br>              <li><strong>dataset</strong> 数据源配置，用于异步获取枚举选项。</li><br>              <li><strong>filter-popper-options</strong> 设置过滤面板弹出层配置项。</li><br>            </ul><br>          </p><br>        </div><br>         | grid/filter/default-filter.vue |
| filter-layout-order-filter | 自定义筛选项顺序 | <p>通过 <code>layout</code> 自定义筛选项的显示顺序和组合。默认值为 <code>input,enum,default,extends,base</code>。可选项：<code>input</code> 输入筛选、<code>enum</code> 枚举选择、<code>default</code> 空/非空、<code>extends</code> 扩展项、<code>base</code> 清除操作、<code>simple</code> 简化版。</p> | grid/filter/layout-order-filter.vue |
| filter-label-value-filter | label/value 字段映射 | <p>通过 <code>label</code> 和 <code>value</code> 配置枚举数据的显示值和实际值字段，适配后端返回的 code/name 等不同字段结构。默认分别为 <code>label</code> 和 <code>value</code>。</p> | grid/filter/filter-label-value.vue |
| filter-dynamic-filter | 动态改变筛选项 | <p>通过修改 <code>filter.values</code> 数组或在 <code>values</code> 中传入函数，可动态改变或异步加载筛选项。</p> | grid/filter/dynamic-filter.vue |
| filter-simple-filter | 简化版筛选 - 单选/多选菜单 | <p>设置 <code>layout: "simple"</code> 启用简化版筛选。支持 <code>selectAll</code> 全选、<code>searchConfig</code> 搜索框。适用于管理侧规范的紧凑筛选面板。</p> | grid/filter/simple-filter.vue |
| filter-simple-date-filter | 简化版筛选 - 日期范围 | <p>设置 <code>layout: "simple"</code> 且 <code>simpleFilter.isDatetime: true</code>，使用 <code>datetimeConfig</code> 配置日期范围选择器，支持 min、max、startDate、endDate 等。</p> | grid/filter/simple-date-filter.vue |
| filter-simple-default-value-filter | 筛选默认选中值 | <p>通过 <code>filter.condition.value</code> 配置筛选的默认选中值，表格初始化时即应用该筛选条件。</p> | grid/filter/simple-default-value-filter.vue |
| filter-input-custom-component | 自定义输入组件 | <p>通过 <code>inputFilter.component</code> 传入自定义组件，实现非标准输入（如范围输入：左最小值、右最大值）。自定义组件需支持 <code>modelValue</code> + <code>update:modelValue</code>，或配置 <code>model</code> 指定字段名。配合 <code>relations[].method</code> 实现自定义筛选逻辑。</p> | grid/filter/input-filter-custom-component.vue |
| filter-advanced-filter | 高级筛选 | <p>通过 <code>inputFilter.component</code> 配置自定义输入组件（如 TinyNumeric、TinyDatePicker）；<code>inputFilter.relations</code> 自定义筛选关系；<code>extends</code> 添加快捷筛选项。</p> | grid/filter/advanced-filter.vue |
| filter-custom-filter | 自定义筛选插槽 | <p>通过 <code>#filter</code> 插槽完全自定义筛选面板 UI。插槽参数提供 <code>context</code>，可调用 <code>commitFilter</code>、<code>resetFilter</code>、<code>clearFilter</code> 等方法。需配合 <code>filter.method</code> 实现筛选逻辑。</p> | grid/filter/custom-filter.vue |
| filter-server-filter | 服务端过滤 | <p>配置 <code>remote-filter</code> 开启服务端过滤。筛选时会调用表格 <code>fetch-data</code> 的 api，并将 filters 参数传入，筛选完成后触发 <code>filter-change</code> 事件。</p> | grid/filter/server-filter.vue |
| server-filter-default | 服务端过滤默认选中值 | <p>服务端过滤下，若需设置默认选中值，需将 <code>auto-load</code> 设为 false，在列初始化后手动调用 <code>handleFetch</code>。否则 <code>handleFetch</code> 在列初始化前执行，无法获取筛选参数。</p> | grid/filter/server-filter-default.vue |
| filter-default-relation | 输入过滤默认选项 | <p>通过 <code>inputFilter.relation</code> 配置输入筛选的默认关系。内置值：<code>equals</code>、<code>contains</code>、<code>startwith</code>、<code>greaterThan</code> 等。自定义 <code>relations</code> 时，可通过 <code>method</code> 配置筛选逻辑。</p> | grid/filter/default-relation.vue |
| filter-request-service-reload-filter | 重载时保持筛选 | <p>调用 <code>handleFetch("reload")</code> 时默认会清除筛选。设置 <code>fetchData.reloadConfig.filter: true</code> 可在重载后保留当前筛选条件。</p> | grid/filter/request-service-reload-filter.vue |
