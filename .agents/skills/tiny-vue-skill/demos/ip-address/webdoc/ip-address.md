# ip-address Demos

| demoId | 名称 | 描述 | 代码文件 |
|--------|------|------|----------|
| basic-usage | 基础用法 | <p>通过 <code>v-model</code> 设置绑定值， <code>type</code> 设置类型，可选值为 <code>IPv4</code> 、<code>IPv6</code> 。</p> | ip-address/basic-usage.vue |
| delimiter | 分隔符 | <p>通过 <code>delimiter</code> 设置分隔符 (图标类)，或通过默认插槽设置分隔符。</p> | ip-address/delimiter.vue |
| readonly | 文本只读 | <p>通过 <code>readonly</code> 设置文本的只读行。</p> | ip-address/readonly.vue |
| disabled | 禁用 | <p>通过 <code>disabled</code> 设置文本是否禁用。</p> | ip-address/disabled.vue |
| size | 尺寸 | <p>通过 <code>size</code> 设置尺寸，可选值有 <code>medium</code> 、<code>small</code> 、<code>mini</code> ，不设置则为默认尺寸。</p> | ip-address/size.vue |
| event | 事件 | <p><br>          文本框获取焦点时触发 <code>focus</code> 事件，失去焦点时触发 <code>blur</code> 事件。<br /><br>          文本框内容改变后触发 <code>change</code> 事件。<br /><br>          文本框输入时触发 <code>input</code> 事件。<br /><br>          文本框内容选中时触发 <code>select</code> 事件。<br /><br>        </p> | ip-address/event.vue |
